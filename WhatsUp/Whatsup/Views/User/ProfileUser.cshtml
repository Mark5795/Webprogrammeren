@model Whatsup.Models.ProfileUserViewModel

@{
    ViewBag.Title = "Profile";
}

<h3>Profile</h3>
<hr />

<section class="form-horizontal">

    <section class="form-group">
        <section class="col-md-offset-1 col-md-10"">
            <p><b>Name:</b> @Model.Username</p>
            <p><b>Email:</b> @Model.Email</p>
            <p><b>Account created:</b> @Model.DateCreated.ToString()</p>
        </section>
    </section>

    <section class="form-group">
        <section class="col-md-10">
            <section class="btn btn-default">
                @Html.ActionLink("Edit Account", "EditUser", "User")
            </section>
            <section class="btn btn-default">
                @Html.ActionLink("Delete Account", "DeleteUser", "User")
            </section>
        </section>
    </section>
</section>

<section class="back-link">
    @Html.ActionLink("Back to Home", "Index", "Home")
</section>
