@model IEnumerable<Whatsup.Models.ContactViewModel>

@{
    ViewBag.Title = "ChatContact";
}

@Html.ActionLink("Add Contact", "AddContact", "Contact", null, new { @class = "btn btn-primary btn-lg", id = "top-button" })
@Html.ActionLink("New GroupChat", "AddGroupChat", "Chat", null, new { @class = "btn btn-primary btn-lg", id = "top-button" })

<hr />

<h2>Contacts to chat</h2>

<section id="Contact">
    @if (Model.Count() == 0)
    {
        <p>You have no contacts, go to "Add Contact" to add contacts!</p>
    }
    else
    {
        foreach (var item in Model)
        {
            @Html.ActionLink(item.NickName, "AddChat", new { contactIndex = item.Index }, new { @class = "ContactListButton" })
            <p></p>
        }
    }

</section>

<section class="back-link">
    @Html.ActionLink("Back to Home", "Index", "Home")
</section>

